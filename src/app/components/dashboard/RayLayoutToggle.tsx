import React from 'react';

interface RayLayoutToggleProps {
  mode: 'floating' | 'native';
  onChange: (mode: 'floating' | 'native') => void;
}

export const RayLayoutToggle: React.FC<RayLayoutToggleProps> = ({ mode, onChange }) => {
  return null;
};
